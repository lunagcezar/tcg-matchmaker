{
  "name": "@tcg/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint --cache . && tsc --noEmit",
    "lint:fix": "eslint --cache . --fix && tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "eslint": "^9.39.4",
    "typescript": "^5.7.0"
  }
}
